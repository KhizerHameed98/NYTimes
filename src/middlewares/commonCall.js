import genericAxiosCall from "../AxiosConfig/genericAxiosCall";
import { serverRoutes } from "../Constants/serverRoutes";
import { handleError } from "./errorHandler";

export const callApi = (url, method, data, params, valueSetter) => {};
